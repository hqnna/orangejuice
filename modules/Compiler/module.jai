// The Compiler API: what a metaprogram sees and says (**C§3**, **C§5.3**).
//
// Split the way the reference splits it, so that anything looking for
// `Compiler/Compiler.jai` finds the declarations where it expects them.

#load "Compiler.jai";
